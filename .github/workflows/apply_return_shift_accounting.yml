name: Apply Return Shift Accounting

on:
  push:
    paths:
      - tool/patch_return_shift_accounting.py

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: audit/accounting-integrity-109
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.0'
          channel: stable
      - name: Apply patch
        run: python tool/patch_return_shift_accounting.py
      - name: Format
        run: dart format lib/features/orders/data/branch_aware_order_repository.dart
      - name: Verify changed source only
        run: git diff --check
      - name: Commit patch
        run: |
          git config user.name 'Tajer Accounting Audit'
          git config user.email 'audit@tajer.local'
          git add lib/features/orders/data/branch_aware_order_repository.dart
          git commit -m 'fix(accounting): post returns to current shift refund ledger'
          git push origin HEAD:audit/accounting-integrity-109
